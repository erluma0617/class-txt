clc, clear
a=zeros(5); a(1,[2:5])=1; %只需考虑非赋权图
a(2,[3 4])=1; a(3,[4 5])=1; a(4,5)=1;
a=a+a'; %输入完整的邻接矩阵
L=diag(sum(a))-a; %计算Laplace矩阵
L(:,end)=[]; L(end,:)=[]; %提出一个n-1阶顺序主子阵
tau=det(L)  %计算顺序主子式，即计算生成树的个数
